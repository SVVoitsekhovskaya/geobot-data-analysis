>>> print(f"Уникальных описаний (ReleveId): {long_data['ReleveId'].nunique(\)}")
Уникальных описаний (ReleveId): 105 # количество описаний на 1 меньше, чем фактически проведенных, т.к. 2 раза работали на одной и той же точке 

>>> print(f"Уникальных видов (SpeciesId): {long_data['SpeciesId'].nunique()\}")
Уникальных видов (SpeciesId): 612

>>> print(f"Общее количество записей: {len(long_data):,}")
Общее количество записей: 64,260
